# html-resume

<!--
	README: This small static site is a personal resume built with vanilla HTML, CSS, and JS.
	Use: Run a static server from the project root to preview pages locally (python -m http.server).
	Branching: Changes are being developed on feature/readme-test. Open PR to merge into main when ready.
-->

Small workflow test: created a feature branch and pushed from local workspace on 2025-11-01.